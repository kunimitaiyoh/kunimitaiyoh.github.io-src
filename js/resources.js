define(() => {
    return {
        common: {
        },
        translations: {
            en: {
            },
            ja: {
            },
        }
    };
});

const errors = (() => {
    const translationKeys = Object.keys(resources.translations)
        .map(key => Object.keys(resources.translations[key]));
});
